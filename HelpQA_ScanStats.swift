import Foundation

// MARK: - HelpQA_ScanStats
struct HelpQA_ScanStats {
    static let items: [HelpQAData.QAItem] = [

        .init(
            id: 28,
            category: .scanStats,
            question: "Q28. Scan Stats の上限に達しました",
            answer: """
A. Scan Stats にはセッション単位の上限があります。

- 種類数：最大 10 種類
- 合計回数：最大 1,000 回

上限に達すると、それ以上は同じセッションで計上できません。
続ける場合は、集計を終了して新しいセッションを開始してください。
"""
        ),

        .init(
            id: 29,
            category: .scanStats,
            question: "Q29. Scan Stats の “リセット” は何が消えますか？",
            answer: """
A. そのセッションの集計結果が消えます。

主に消える内容は次のとおりです。
- 合計スキャン数
- 種類数
- コードごとの内訳

リセット後は最初から集計し直しになります。
"""
        ),

        .init(
            id: 30,
            category: .scanStats,
            question: "Q30. 未購入でも Scan Stats は使えますか？",
            answer: """
A. はい。未購入でもスキャン自体はできます。

ただし未購入時は、有料利用とは動きが異なります。

- スキャン体験はできます
- 内部的な計上処理は進みます
- 最初の 5 回までは preview 表示を体験できます
- 6 回目以降は preview 表示できません

未購入状態では、本番利用向けの継続的な集計表示を行う前提ではありません。
継続して Scan Stats を使う場合は購入してください。
"""
        ),

        .init(
            id: 31,
            category: .scanStats,
            question: "Q31. Scan Stats で同じコードを続けて計上したい",
            answer: """
A. 現在は手動確定方式です。

候補が表示された状態で中央の丸ボタンを押すと、その時点で計上されます。
そのため、同じコードが画面内に残っていても、ボタンを押すたびに再計上できます。

ただし次の場合は計上できません。
- 複数のコードが同時に映っている場合（候補が出ません）
- セッション上限に達している場合

確実に計上するには、枠内に 1 つのコードだけを表示してください。
"""
        ),

        .init(
            id: 32,
            category: .scanStats,
            question: "Q32. Scan Stats の結果を保存・出力できますか？",
            answer: """
A. 現在のバージョンでは、常設の保存・出力機能は前提ではありません。

ただし、作業終了時の最終集計は画面で確認できます。
共有や出力の導線が画面上に表示されている場合は、その案内に従ってください。
表示がない場合は、そのバージョンでは未対応です。
"""
        ),
    ]
}
